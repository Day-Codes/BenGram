import { GuildMember, User } from "discord.js";
import MusicTypes from "../interfaces/MusicTypes/MusicTypes";
export interface PlaylistBaseInformation {
    list?: {
        owner: string;
        name?: string;
        imageURI?: string;
        source?: "Youtube" | "Spotify";
    };
    tracks?: MusicTypes[];
}
declare function getPlaylist(uri: string, member: GuildMember | User): Promise<PlaylistBaseInformation | {
    list: {
        imageURI: any;
        name: any;
        owner: string;
        source: "Spotify";
    };
    tracks: MusicTypes[];
}>;
export default getPlaylist;
//# sourceMappingURL=playlists.d.ts.map